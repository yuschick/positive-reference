import authModel, { IAuthModel } from './model';
import { IAuthModelThunks } from './thunks';
import { RequestType } from './requests';

export default authModel;
export { IAuthModel, IAuthModelThunks, RequestType as AuthRequestType };
