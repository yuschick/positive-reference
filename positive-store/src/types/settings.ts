export enum Language {
  fi = 'fi',
  en = 'en',
}

export enum Theme {
  light = 'light',
  dark = 'dark',
}

export enum Environment {
  development = 'development',
  staging = 'staging',
  production = 'production',
}
